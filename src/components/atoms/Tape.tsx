import React from 'react';

const Tape: React.FC = () => {
  return (
    <div
      className="green-strype h-4 w-full"
      style={{
        background: 'radial-gradient(circle, rgba(76,148,4,1) 15%, rgba(52,92,4,1) 85%)',
      }}
    />
  );
};

export default Tape;
